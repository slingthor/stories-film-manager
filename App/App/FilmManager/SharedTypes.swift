import Foundation

// MARK: - Shared Types

// Plate Type enum used across multiple views
enum PlateType: String, CaseIterable {
    case character = "Characters"
    case environment = "Environments"
}